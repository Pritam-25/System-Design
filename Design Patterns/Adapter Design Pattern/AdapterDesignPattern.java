//! Adapter Design Pattern
// Adapter converts the interface of a class into another interface that client expects .
// Adapter lets classes work together that could'nt otherwise because of incompatible interface.

public class AdapterDesignPattern {
    // object adapter
    // class adapter (not recommendable)
}
